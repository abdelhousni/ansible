- variable name must begin with an alphabetical
- only punctuation allowed is the underscore 
- when a key's value BEGINS with a variable, encapsultation is required with ""

valid variable names:
  - {{ my_string }}
  - {{http_port}}
  - nfs_port_1

reference variables using {{ }}

firewalld:
  service: "{{ http_service }}"
