[defaults]
inventory = inventory
remote_user = devops
ask_pass = false
log_path = logs/ansible.log
ansible_keep_remote_files = true

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
